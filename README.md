# DecodeLabs AI Internship — Batch 2026
**Intern:** Monika Dey | [@monika2671](https://github.com/monika2671)

Three AI projects built during the DecodeLabs Industrial Training Program, progressing from rule-based logic → supervised learning → recommendation systems.

---

## Project 1: Rule-Based AI Chatbot
**File:** `project_1.py`

Chatbot that matches user input against a dictionary of 8 intents using Python's `.get()` method. Runs in a continuous loop with input sanitization and a fallback response for unknown inputs.

**Skills:** Control flow, dictionary-based O(1) lookup, input normalization

---

## Project 2: Iris Flower Classifier (KNN)
**File:** `project_2.py`

KNN classifier trained on the Iris dataset (150 samples, 3 classes, 4 features). Achieved 100% accuracy with perfect precision, recall, and F1-score across all classes.

**Skills:** Supervised learning, StandardScaler, train-test split, confusion matrix

---

## Project 3: Tech Stack Recommender
**File:** `project_3.py`

Content-based recommendation engine that takes 3 user skills and returns the Top 3 most relevant tech career paths using TF-IDF vectorization and Cosine Similarity scoring.

**Skills:** TF-IDF, cosine similarity, content-based filtering, recommendation pipeline

---

## Stack
Python 3 | scikit-learn | Git & GitHub | VS Code